{
  "private": true,
  "scripts": {
    "dev": "elm-live -d public src/Main.elm -- --output=public/index.html",
    "debug": "npm run dev -- --debug",
    "build": "elm make src/Main.elm --output=public/index.html --optimize"
  },
  "dependencies": {
    "elm": "^0.19.1-5",
    "elm-live": "^4.0.2"
  }
}
